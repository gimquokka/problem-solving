import sys

if a1 == b1==1:
    if int(f) <= k and int(t) >= k:
        return 1
    else:
        return 0


    if f == t:

        for i in range(l):
            if t[i] == str(k): nums += 1
        return nums
    elif a1 == 1:
        if int(f) <= k and int(t) >= k:
            return 1
        else:
            return 0